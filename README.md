# 🧠 Предсказание цен на недвижимость с PyTorch, Optuna и MLflow 🏠

Это комплексный MLOps-проект по предсказанию стоимости жилья на основе данных из соревнования [Kaggle "House Prices - Advanced Regression Techniques"](https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques).

Проект демонстрирует полный цикл работы над DS-задачей: от глубокой предобработки и инжиниринга признаков до автоматического подбора гиперпараметров, отслеживания экспериментов и создания воспроизводимой среды с помощью **PyTorch, Optuna, MLflow и Docker**.

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg)](https://www.python.org/)
[![PyTorch](https://img.shields.io/badge/PyTorch-2.0%2B-orange.svg)](https://pytorch.org/)
[![MLOps Stack](https://img.shields.io/badge/MLOps-Optuna%20%7C%20MLflow%20%7C%20Docker-brightgreen.svg)](https://mlflow.org/)
[![Kaggle Score (RMSLE)](https://img.shields.io/badge/Kaggle%20Score%20(RMSLE)-0.12842-blue.svg)](https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques)

---

## 🌟 Ключевые Особенности Проекта

*   **Продвинутый MLOps-стек:**
    *   **Optuna** для автоматического подбора гиперпараметров нейронной сети.
    *   **MLflow** для логирования экспериментов, сохранения артефактов (моделей, параметров) и версионирования.
    *   **Docker** и **Docker Compose** для создания полностью изолированной и воспроизводимой среды, включающей Jupyter, MLflow UI и Optuna Dashboard.

*   **Визуальный Анализ Данных в Power BI:**
    *   Отдельный интерактивный дашборд (`house_prices_eda.pbix`) для глубокого исследовательского анализа данных (EDA).

*   **Автоматический Отбор Признаков (Automated Feature Selection):**
    *   В процессе подбора гиперпараметров **Optuna** также определяет оптимальное количество "слабых" признаков, которые следует удалить для улучшения качества модели. Этот процесс полностью автоматизирован.

*   **Динамическая Архитектура Модели на PyTorch:**
    *   Нейронная сеть `DynamicRegressionModel` строится "на лету" на основе параметров, предложенных Optuna: количество слоев, число нейронов, dropout, функция активации (`ReLU`, `GELU`, `SiLU` и др.).

*   **Глубокий Инжиниринг Признаков (Feature Engineering):**
    *   Более 30 новых признаков, созданных на основе исходных данных: временные (`YearBuildAgo`), порядковые (`ExterQual`), агрегированные и условные (`Is_Rec_Room`, `Alley_road`).
    *   Комплексная обработка пропусков и редких категорий.

*   **Надёжная Оптимизация:**
    *   Использование продвинутого оптимизатора `RAdam` и кастомного прунера `MovingAveragePruner` для сглаживания "шумных" метрик и более стабильного отсечения неудачных экспериментов Optuna.

---

## ⚙️ Архитектура и MLOps-пайплайн

Весь процесс выстроен в единый автоматизированный пайплайн:

1.  **Запуск в Docker:** `docker-compose up` поднимает три взаимосвязанных сервиса: JupyterLab для работы с кодом, MLflow UI для просмотра экспериментов и Optuna Dashboard для визуализации процесса оптимизации.
2.  **Оптимизация в Optuna:** Запуск ноутбука инициирует `study` в Optuna. Для каждого `trial` (эксперимента) Optuna предлагает новый набор гиперпараметров, включая архитектуру сети и количество удаляемых признаков.
3.  **Обучение и Валидация:** Модель обучается на предложенных параметрах. Метрика качества (RMSLE) на валидационной выборке возвращается в Optuna.
4.  **Поиск лучшей модели:** Optuna проводит сотни таких экспериментов, находя оптимальную комбинацию.
5.  **Логирование в MLflow:** После завершения оптимизации лучшая модель вместе со всеми её параметрами, метриками и списком удалённых признаков сохраняется в MLflow.
6.  **Создание Submission:** Финальный скрипт загружает лучшую модель и артефакты из MLflow, применяет нужные преобразования к тестовым данным и генерирует итоговый файл для Kaggle.

---

## 🏆 Результаты Оптимизации

Итоговый результат на приватном лидерборде Kaggle составил **`0.12842`**.

*   **Лучшая метрика на локальной валидации (Validation RMSLE): `0.1165`**
    *   *RMSLE (Root Mean Squared Logarithmic Error) — это корневая среднеквадратичная ошибка логарифмов. Она лучше подходит для задач с ценами, так как штрафует за относительную, а не абсолютную ошибку.*

Примечательно, что финальный результат на Kaggle немного выше (хуже), чем лучший результат на нашей валидационной выборке. Это типичная и важная ситуация в машинном обучении, которая может указывать на небольшое **переобучение (overfitting)** на валидационный сет или на **смещение распределения (distribution shift)** между данными, использованными для валидации, и приватным тестовым набором данных Kaggle.

*   **Оптимальные гиперпараметры, найденные Optuna:**
    *   **`num_cols_to_drop`**: `30` (модель работает лучше всего, когда удалены 30 самых слабых признаков)
    *   **`activation_fn`**: `'gelu'`
    *   **`num_hidden_layers`**: `1`
    *   **`n_units_l0`**: `64`
    *   **`dropout_l0`**: `0.2`
    *   **`learning_rate`**: `0.004`
    *   **`batch_size`**: `64`
    *   **`weight_decay`**: `0.00186`

---

## 🗂️ Структура Проекта

```
.
├── data/
│   ├── train.csv                 # Обучающие данные
│   ├── test.csv                  # Тестовые данные
│   ├── sample_submission.csv     # Пример файла для отправки
│   ├── data_description.txt      # Официальное описание полей (EN)
│   └── data_descripton_ru.md     # Описание полей (RU)
├── reports/
│   └── house_prices_eda.pbix     # Интерактивный дашборд для EDA в Power BI
├── .dockerignore
├── .gitignore
├── Dockerfile
├── docker-compose.yml
├── kaggle-house-prices-analysis.ipynb # Основной Jupyter Notebook с пайплайном
├── new_kaggle_house_prices.ipynb # (Черновая или альтернативная версия)
├── optuna_study.db               # База данных с результатами экспериментов Optuna
├── README.md                     # Этот файл
├── requirements.txt              # Список Python-зависимостей
├── dropped_columns.txt           # Список колонок, удаленных лучшей моделью
└── submission_house_prices.csv   # Итоговый файл с предсказаниями
```

---

## 🚀 Запуск и Использование

Проект полностью контейнеризирован, что делает запуск максимально простым и независимым от вашей локальной системы.

### Требования

*   **Docker**
*   **Docker Compose**
*   **NVIDIA GPU** (рекомендуется для ускорения обучения, но проект запустится и на CPU)
*   **Power BI Desktop** (для просмотра `reports/house_prices_eda.pbix`)

### Пошаговая инструкция

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
    cd YOUR_REPO_NAME
    ```

2.  **Подготовьте данные:**
    *   Скачайте файлы `train.csv` и `test.csv` со страницы [Kaggle Competition](https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques/data) и поместите их в папку `data/`.

3.  **Соберите и запустите сервисы:**
    (Убедитесь, что Docker Desktop или Docker Engine запущен)
    ```bash
    docker-compose up --build
    ```
    Эта команда автоматически:
    *   Соберёт Docker-образ с установленными зависимостями из `requirements.txt`.
    *   Запустит три контейнера: `jupyter_lab`, `mlflow_server` и `optuna_dashboard`.
    *   Смонтирует текущую директорию внутрь контейнеров, так что все изменения в коде и сгенерированные файлы будут доступны на вашем компьютере.

4.  **Начните работу:**
    Откройте браузер и перейдите по следующим адресам:
    *   **JupyterLab**: `http://localhost:8888`
        *   Откройте ноутбук `kaggle-house-prices-analysis.ipynb` и запускайте ячейки последовательно.
    *   **MLflow UI**: `http://localhost:5000`
        *   Здесь будут появляться завершённые эксперименты. Вы сможете просматривать параметры, метрики и скачивать артефакты (например, обученную модель).
    *   **Optuna Dashboard**: `http://localhost:8081`
        *   Здесь можно в реальном времени отслеживать процесс подбора гиперпараметров, смотреть графики важности параметров и историю `trial`-ов.

5.  **Остановка:**
    В терминале, где вы запустили `docker-compose up`, нажмите `Ctrl+C`. Затем для полной остановки и удаления контейнеров выполните:
    ```bash
    docker-compose down
    ```
---

## 💡 Возможные Улучшения и Направления для Развития

Этот проект закладывает мощную MLOps-основу, на базе которой можно проводить дальнейшие эксперименты для улучшения итогового результата. Вот ключевые направления для развития:

*   **Внедрение Кросс-Валидации (Cross-Validation)**
    *   **Проблема:** Текущая оценка на единственном валидационном сплите (полученном через `train_test_split`) может быть нестабильной и сильно зависеть от `random_state`.
    *   **Решение:** Заменить `train_test_split` на `KFold` или `StratifiedKFold` (для задач классификации). Обучать модель K раз на разных фолдах и усреднять предсказания (для `submission`) и метрики (для оценки). Это сделает оценку качества модели более робастной и снизит риск переобучения на конкретный валидационный сет.

*   **Использование Модельно-Ориентированного Отбора Признаков**
    *   **Проблема:** Отбор признаков на основе корреляции с целевой переменной — это хороший, но базовый метод. Он не учитывает нелинейные зависимости и взаимодействия между признаками.
    *   **Решение:** Использовать более продвинутые техники, которые опираются на уже обученную модель:
        *   **Permutation Importance:** Оценивает падение качества модели после случайного перемешивания значений одного признака.
        *   **SHAP Values:** Показывает вклад каждого признака в предсказание для каждого отдельного объекта, давая глубокое понимание его влияния.
        *   **Recursive Feature Elimination (RFE):** Рекурсивно удаляет самые слабые признаки.

*   **Эксперименты с Альтернативными Архитектурами и Ансамблирование**
    *   **Проблема:** Простая полносвязная нейросеть (MLP) — не всегда лучшее решение для табличных данных.
    *   **Решение:**
        *   **Градиентный Бустинг:** Попробовать state-of-the-art модели для табличных данных, такие как **LightGBM**, **XGBoost** или **CatBoost**. Они часто показывают превосходные результаты "из коробки".
        *   **Ансамблирование (Ensembling/Stacking):** Создать гибридную модель, объединив предсказания нейронной сети и модели градиентного бустинга. Это классический приём для победы в Kaggle-соревнованиях, так как разные модели хорошо улавливают разные типы зависимостей в данных.

*   **Обогащение Пространства Признаков (Feature Engineering)**
    *   **Проблема:** Модель ограничена информацией, уже присутствующей в данных.
    *   **Решение:** Генерировать новые, более сложные признаки:
        *   **Полиномиальные признаки и взаимодействия:** Создать комбинации самых важных признаков (например, `OverallQual * GrLivArea`).
        *   **Агрегаты по группам:** Рассчитать статистические показатели (среднее, медиану, стандартное отклонение) цен или площадей в разрезе, например, района (`Neighborhood`). Это даст модели контекст о "престижности" или "типичности" района.

*   **Аугментация Данных и Псевдо-Лейблинг (Pseudo-Labeling)**
    *   **Проблема:** Обучающий набор данных имеет ограниченный размер.
    *   **Решение:** Использовать тестовые данные для дообучения модели.
        1.  Обучить модель на `train.csv`.
        2.  Сделать предсказания для `test.csv`.
        3.  Выбрать предсказания, в которых модель наиболее уверена (например, 10-20% самых высоких и самых низких цен).
        4.  Добавить эти "псевдо-размеченные" данные из `test.csv` к обучающему набору и запустить обучение заново. Это может помочь модели лучше адаптироваться к распределению тестовых данных.
---

Автор: **Vasja1142**
GitHub: [https://github.com/Vasja1142](https://github.com/Vasja1142)
